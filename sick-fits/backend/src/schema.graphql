# import * from './generated/prisma.graphql'

type Mutation {
  createItem(Item: String, title: String, description: String, price: Int, image: String, largeImage: String): Item!
}
type Query {
  items: [Item]!
}